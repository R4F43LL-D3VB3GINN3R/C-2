Elabore um programa em C++ que tendo por base a largura e altura, com as seguintes funções:

-Implemente a função int area(int altura, int largura) que retorna o valor da area correspodente ao retangulo dado.

-Implemente a função void imprimirRetangulo (int altura, int largura), que desenha o retangulo no ecra, 
 utilizando os caracters "|" (para cada unidade de altura), "--" (para cada unidade de largura) e "+" (para os vertices), 
 sempre que a altura e a largura tiverem valore menores que 10; 
 caso contrario imprima no seguinte formato texto "altura: <valor> largura: <valor>"

-Implemente a funçao void girar (int &altura, int&largura), que realiza um rotação de 90º no retangulo, mudificando-o.

quando o renatngulo gira, tambem tem de se desenhar o retangulo
